dhcp:
  status:
    cmd: systemctl status isc-dhcp-server
    help: Display the status of the DHCP server   
  start:
    cmd: systemctl start isc-dhcp-server
    help: Start the DHCP server   
  stop:
    cmd: systemctl stop isc-dhcp-server
    help: Stop the DHCP server   
